{
  "name": "attribute-3-data-collection",
  "version": "1.0.0",
  "private": true,
  "description": "Production-grade institutional data collection system for Attribute 3 (Standalone Frontend)",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "postinstall": "npm --prefix frontend install"
  }
}
